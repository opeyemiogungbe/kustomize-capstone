resources:
  - ../../base
patches:
  - path: replica_count.yaml
  - path: deployment_patch.yaml
labels:
  - pairs:
      env: development # Label applied to every resource
    includeSelectors: true
namePrefix: dev-  # Prefix added to the name of all resources
